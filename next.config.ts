import type { NextConfig } from "next"

const nextConfig: NextConfig = {
  // Processar o dataset do Apify roda em route handlers; não há páginas
  // estáticas que dependam de dados, então nada especial por enquanto.
}

export default nextConfig
